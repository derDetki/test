#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

typedef struct treenode_s{
    int data;
    struct treenode_s *left;
    struct treenode_s *right;
    
        /* data */
    } TreeNode;

TreeNode*

insert (TreeNode* root, int value);
void print_inorder(TreeNode* root);
void print_preorder(TreeNode* root);
void print_postorder(TreeNode* root);

int main (void){

}

insert (TreeNode* root, int value){
    if (!root)

}